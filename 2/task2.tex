\documentclass[12pt]{article}
\usepackage[cp1251]{inputenc}
\usepackage[russian]{babel}
\usepackage{amsmath}
\usepackage{amsfonts}
\setlength{\parindent}{5ex}
\setlength{\parskip}{1em}

\begin{document}
Писать $\tilde i$ некрасиво; лучше писать так: $\tilde\imath$.

Правильно $\Hat{\Hat Z}$, а не $\hat{\hat Z}$.

В формуле $\mathrm{tg} x$ буква $x$ слишком близка к знаку тангенса. А вот в формуле $\sin x$ пробелы правильные.

Множество особенностей многообразия $X$ обозначается $X_{\mathrm{sing}}$.

Раньше вместо~$\Gamma^k_{ij}$ писали~$\left\{ij\atop k\right\}$.

$$M(f)=\left.\left(\int\limits_a^bf(x)\,dx\right)\right/(b-a)$$

$$\int\limits_a^b\frac12(1+x)^{-3/2}dx=\left.-\frac{1}{\sqrt{1+x}}\right|_a^b$$

$$\left| |x+1|-|x-1|\right|$$

$$\bigl| |x+1|-|x-1|\bigr|$$

$$\left(\sum_{k=1}^n x^k\right)^2$$

$$\Bigl(\sum_{k=1}^n x^k\Bigr)^2$$

Множество $\{x\mid x\not\ni x\}$ существовать не может. В этом состоит парадокс Рассела.

$$\boxed{\iint_{\mathbb R^2}e^{-(x^2+y^2)}\,dx\,dy=\pi}$$

$$\overline{a_na_{n-1}\ldots a_1a_0}=10^na_n+\ldots+a_0.$$

\begin{center}
$\widehat{f*g}=\hat f\cdot\hat g$ Рассмотрим вектор $\overrightarrow{AB}$
\end{center}

\begin{center}
$\frac23$ и $\dfrac23$
\end{center}

\begin{center}
$2^{\frac35}$ и $2^{\tfrac35}$
\end{center}

\begin{center}
$\binom{12}7=792$
\end{center}

$${n\choose k}=\frac{n!}{k!(n-k)!}$$

$$\frac{\sum\limits_{i=1}^n |x_i||y_i|}{\bigg(\sum\limits_{j=1}^n |x_j|^{\frac{1}{1-\sigma}}\bigg)^{1-\sigma} \bigg(\sum\limits_{j=1}^n |y_j|^{\frac{1}{\sigma}} \bigg)^{\sigma}} \leq {1}$$

$$\textmd{sp}\;A\subseteq \bigcup^n_{i=1} S(a_{ii},r_i),\;r_i=\min\{p_i,q_i\}.$$

$$\left| |x|\right|_{\frac12}=\left(\sum_{i=1}^n |x_i|^2 \right)^{\frac12}.$$

$$e^{tA}=I+\sum_{k=1}^{\infty}\frac{t^kA^k}{k!},$$

$$\lim\limits_{0<t\to0}\frac{\ln\left||e^{tA}|\right|}{t}=\lim\limits_{0<t\to0}\frac{\ln\left| |I+tA|\right|}{t},\quad \left| |A|\right|=\max\limits_{x \neq 0}\frac{\left| |Ax|\right|}{\left| |x|\right|}.$$

\end{document} 